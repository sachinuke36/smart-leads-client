# JWT Secret (IMPORTANT: Change this in production!)
JWT_SECRET=secretkey

# MongoDB Connection (used by docker-compose)
MONGO_URI=mongodb://mongodb:27017/smart-leads

# Server Configuration
PORT=5000
NODE_ENV=production

# JWT Configuration
JWT_EXPIRES_IN=7d

# CORS Configuration
CORS_ORIGIN=http://localhost
